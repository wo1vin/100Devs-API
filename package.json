{
  "name": "100devs-class-info-api",
  "version": "1.0.0",
  "description": "API that lists the resources for each 100Devs class",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "wo1vin",
  "license": "ISC",
  "dependencies": {
    "dotenv": "^16.0.1",
    "env": "^0.0.2",
    "express": "^4.18.1"
  }
}
